Wire LEDs to pin PB4 ( = D12 on Uno/Nano, = D8 on Leonardo/Promicro). (*)

Example example1 will have 3 LEDs show red, green and blue, respectively.

Example example2 will have 1 LED blink.

Example example3 will have 1 LED "pulse" (change brightness) for 20 times.

(*)
Preconfigured pin in ws2812b_config.h is PB4, preset is 3 LEDs. You might
change both settings by editing this file (type "make config" if in doubt).

  
